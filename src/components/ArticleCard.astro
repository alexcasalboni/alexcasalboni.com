---
interface Props {
  title: string;
  url: string;
  source: string;
  summary: string;
}

const { title, url, source, summary } = Astro.props;
---

<article class="card glass">
  <div class="meta">
    <span class="pill">{source}</span>
  </div>
  <h3>
    <a href={url}>{title}</a>
  </h3>
  <p>{summary}</p>
  <a class="read" href={url}>Read the article →</a>
</article>

<style>
  .card {
    padding: 1.25rem;
    border: 1px solid var(--border);
    border-radius: 16px;
    display: grid;
    gap: 0.35rem;
  }

  .meta {
    display: flex;
    justify-content: flex-start;
  }

  .pill {
    padding: 0.35rem 0.7rem;
    border-radius: 10px;
    border: 1px solid var(--border);
    background: rgba(255, 255, 255, 0.03);
    color: var(--muted);
    font-size: 0.9rem;
  }

  h3 {
    margin: 0;
    font-size: 1.2rem;
  }

  p {
    margin: 0;
    color: var(--muted);
  }

  .read {
    font-weight: 600;
  }
</style>
